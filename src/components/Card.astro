<section class="card-section">
  <div class="card-grid">
    <slot />
  </div>
</section>

<style>
  .card-section {
    padding: 3rem 1.5rem; /* space above AND below */
  }

  .card-grid {
    display: grid;
    grid-template-columns: 1fr; /* mobile */
    gap: 1.75rem; /* space between cards */
    max-width: 1100px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .card-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
